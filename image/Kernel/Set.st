"Set — hash と = で引く開番地法のハッシュ表（SPEC §3.6）。エントリは element hash。実行定義は C++ NativeMethod（runtime/src/kernel/Dictionary.cpp）。"

!Set methodsFor: 'adding'!
!Set methodsFor: 'testing'!
!Set methodsFor: 'enumerating'!
