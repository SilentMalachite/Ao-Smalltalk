"ProcessorScheduler — quiescentProcesses / activeProcess。グローバル Processor はこのクラスのインスタンス。実行定義は C++ NativeMethod（runtime/src/kernel/Process.cpp）。"

!ProcessorScheduler methodsFor: 'accessing'!
!ProcessorScheduler methodsFor: 'process state change'!
