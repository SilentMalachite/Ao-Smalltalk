"WriteStream — writeLimit。nextPut: はここにある（SPEC §3.6）。Array は 2 倍にコピーして伸ばし、String は UTF-8 のまま NUL の予備を倍々に確保する。実行定義は C++ NativeMethod（runtime/src/kernel/Stream.cpp）。"

!WriteStream methodsFor: 'accessing'!
!WriteStream methodsFor: 'writing'!
