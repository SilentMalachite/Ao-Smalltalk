# P4-01: Object / UndefinedObject / Boolean

- 親フェーズ: [P4](../phases/P4.md)
- 状態: done
- 依存: P3 完了

## 目的

SPEC §3.6 Kernel-Objects。`.st` はコメントとプロトコルのみ。

## Files

- Create: `runtime/src/kernel/Object.cpp`
- Create: `image/Kernel/Object.st（コメントのみ）`

## Interfaces

- Consumes: send / NativeMethod / Bootstrap
- Produces: SPEC 箇条書きの Object / Boolean セレクタ

## テスト

nil isNil、true ifTrue: 等。インタプリタを通らない。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 Smalltalk ソースを実行定義にしない。

## コミット文

```
Add Object Boolean native methods

Graphify: path NativeMethod Object
Serena: find_symbol ao_Object
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- Kernel を触るので走査テストがあるなら緑（P4-09 以降必須）
