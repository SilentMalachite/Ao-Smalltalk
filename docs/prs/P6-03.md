# P6-03: ブロックと ao --test

- 親フェーズ: [P6](../phases/P6.md)
- 状態: planned
- 依存: P6-02

## 目的

ブロック引数・返り値・外側 temps。`image/tests/*.st` を `ao --test`。

## Files

- Modify: `runtime/src/Interpreter.cpp`, `runtime/src/ao_main.cpp`
- Create: `runtime/tests/block_test.cpp`
- Create: `image/tests/example.st`

## Interfaces

- Consumes: Interpreter
- Produces: `ao --test` 終了コード。

## テスト

`block_test`; ゴールデン example（`1+2`、`ifTrue:ifFalse:`、`Object new class`）。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add blocks and ao --test runner

Graphify: path ao --test Interpreter
Serena: find_symbol main
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

