# P0-03: CMake runtime/compiler/CLI + GoogleTest

- 親フェーズ: [P0](../phases/P0.md)
- 状態: done in P0 execution
- 依存: P0-02

## 目的

C++20 がビルドでき、空の GoogleTest が緑。`ao --help` / `--version`。

## Files

- Create: `CMakeLists.txt`, `runtime/CMakeLists.txt`, `compiler/CMakeLists.txt`
- Create: `runtime/include/ao/Runtime.hpp`, `runtime/src/Runtime.cpp`, `runtime/src/ao_main.cpp`
- Create: `compiler/include/ao/Compiler.hpp`, `compiler/src/Compiler.cpp`
- Create: `runtime/tests/smoke_test.cpp`, `compiler/tests/smoke_test.cpp`
- Create: `scripts/build.sh`, `scripts/test.sh`

## Interfaces

- Consumes: ディレクトリ骨格
- Produces: `ao::boot()`, `ao::shutdown()`, `ao::version_string(char*, int)`, `ao::compiler::version()`, 実行ファイル `build/ao`

## テスト

`Smoke.BootAndShutdownReturnZero`, `Smoke.VersionStringIsNonEmpty`, `CompilerSmoke.VersionIsNonEmpty`。先にテスト、CMake がソース無しで失敗することを確認してから実装。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

GoogleTest は FetchContent v1.15.2 を runtime だけで MakeAvailable。compiler は再 Fetch しない。`ao` の RUNTIME_OUTPUT_DIRECTORY は `${CMAKE_BINARY_DIR}`。

## コミット文

```
Add CMake runtime compiler CLI and smoke tests

Graphify: n/a
Serena: n/a
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- `./build/ao --version` が `0.0.0-p0`
