# P6-02: インタプリタループ

- 親フェーズ: [P6](../phases/P6.md)
- 状態: planned
- 依存: P6-01

## 目的

スタック / send / jump / return / block / primitive 失敗時本文。

## Files

- Create: `runtime/include/ao/Interpreter.hpp`
- Create: `runtime/src/Interpreter.cpp`
- Create: `runtime/tests/compiler_roundtrip_test.cpp`

## Interfaces

- Consumes: Bytecode, Context, send
- Produces: `Interpreter::run(CompiledMethod) -> Oop`

## テスト

`compiler_roundtrip_test`: ソース → バイトコード → 評価。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 Kernel はループに入らない。評価 ABI を足すなら SPEC §3.10 を先に更新。

## コミット文

```
Add bytecode interpreter

Graphify: path Interpreter send
Serena: find_symbol ao::Interpreter
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

