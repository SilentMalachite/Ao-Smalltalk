# P6b-01: Cuis pin と ORIGIN.md

- 親フェーズ: [P6b](../phases/P6b.md)
- 状態: planned
- 依存: P6 完了

## 目的

upstream リビジョンを確定する。未確定のまま file-in しない。

## Files

- Create: `image/vendor/ORIGIN.md`
- Create: `image/vendor/<origin>/`（ソース配置）
- Create: `NOTICE`（混在ライセンス）

## Interfaces

- Consumes: なし（ピン作業）
- Produces: URL・コミット・ライセンス全文の場所。

## テスト

ORIGIN.md に 3 項目がある。ライセンスファイルが同梱。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 Pharo VM / BitBlt / Xerox .im を入れない。

## コミット文

```
Pin Cuis vendor origin

Graphify: query vendor ORIGIN
Serena: n/a (docs)
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

