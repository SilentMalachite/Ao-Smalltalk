# P0-07: Graphify 初期グラフ

- 親フェーズ: [P0](../phases/P0.md)
- 状態: done in P0 execution
- 依存: P0-03, P0-04, P0-05, P0-06

## 目的

CLAUDE.md がセッション開始時に読めと命じる初期グラフをコミットする。

## Files

- Create: `graphify-out/graph.html`
- Create: `graphify-out/GRAPH_REPORT.md`
- Create: `graphify-out/graph.json`

## Interfaces

- Consumes: ここまでのソースと docs
- Produces: GRAPH_REPORT

## テスト

3 成果物が存在。`PHASE` が P0 のまま。`./scripts/test.sh` 緑。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

`graphify-out/graph.json` が無いのでフルパイプライン。失敗したらコードを増やさない。`PHASE` を進めない。

## コミット文

```
Add initial Graphify graph

Graphify: /graphify .
Serena: project.yml present
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- graphify-out を gitignore していない
