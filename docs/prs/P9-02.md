# P9-02: Browser accept

- 親フェーズ: [P9](../phases/P9.md)
- 状態: planned
- 依存: P9-01

## 目的

メソッド accept で辞書更新。クラス定義テキスト accept で作成/再定義。直後の Do it に反映。失敗時テキスト非破壊。

## Files

- Modify: `app/Ao/BrowserWindow.swift`
- Create: `app/AoTests/AcceptTests.swift`

## Interfaces

- Consumes: compiler + ABI accept
- Produces: ユーザーが `Object>>foo` を追加できる。

## テスト

accept 成功と失敗。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add Browser accept

Graphify: path accept CompiledMethod
Serena: find_symbol accept
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

