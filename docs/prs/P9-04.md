# P9-04: v1 ゴールデン受け入れ

- 親フェーズ: [P9](../phases/P9.md)
- 状態: planned
- 依存: P9-03

## 目的

SPEC §6 の全チェック。`.aoimage` 再起動で追加メソッドが残る。プロセス項目（GRAPH_REPORT、Serena で Object 解決）。

## Files

- Modify: `image/tests/*.st`
- Modify: `PHASE` → `P9`

## Interfaces

- Consumes: 全先行フェーズ
- Produces: v1 完了。

## テスト

SPEC §6 チェックリストをテストまたは手動手順に割り当てた記録。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 未達の性能は SPEC に理由を書く。deep Graphify。

## コミット文

```
Record v1 acceptance and set PHASE P9

Graphify: . --mode deep
Serena: find_symbol ao_Object_identityEquals
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- PHASE が P9
- SPEC §6 がすべて満たされている
