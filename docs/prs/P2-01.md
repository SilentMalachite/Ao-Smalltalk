# P2-01: WellKnown と即値クラス

- 親フェーズ: [P2](../phases/P2.md)
- 状態: planned
- 依存: P1 完了

## 目的

nil/true/false の即値と、テストから名前で参照する well-known 表。

## Files

- Create: `runtime/include/ao/WellKnown.hpp`
- Create: `runtime/src/WellKnown.cpp`
- Create: `runtime/tests/bootstrap_test.cpp`（即値ケースから）

## Interfaces

- Consumes: Oop, Heap, Roots
- Produces: `WellKnown::nil()`, `true_()`, `false_()`, 名前引き。

## テスト

即値の `#class` 相当が後続 PR で結べるよう、クラス OOP スロットを予約。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add well-known immediates

Graphify: query WellKnown
Serena: find_symbol ao::WellKnown
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

