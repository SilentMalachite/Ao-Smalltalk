# P3-03: NativeMethod とセレクタマングル

- 親フェーズ: [P3](../phases/P3.md)
- 状態: planned
- 依存: P3-02

## 目的

関数ポインタ + argc + 任意 primitive 番号 + 安定シンボル名。

## Files

- Create: `runtime/include/ao/NativeMethod.hpp`
- Create: `runtime/src/NativeMethod.cpp`
- Create: `docs/native-selectors.md`

## Interfaces

- Consumes: MethodDictionary
- Produces: `NativeMethod` オブジェクト。マングル規則文書。例: `ao_Object_identityEquals`, `ao_SmallInteger_add`。

## テスト

Inspect 相当に名前が載る。実行はまだ lookup 経由（P3-04）。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 ポインタをイメージに書かない設計を文書に明記。

## コミット文

```
Add NativeMethod and selector mangling doc

Graphify: query NativeMethod
Serena: find_symbol ao::NativeMethod
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- `docs/native-selectors.md` がある
