# P0-01: git / LICENSE / gitignore / PHASE / README

- 親フェーズ: [P0](../phases/P0.md)
- 状態: done in P0 execution
- 依存: なし

## 目的

リポジトリの法務ファイルと P0 マーカを置く。

## Files

- Create: `LICENSE` (MIT)
- Create: `.gitignore`（`graphify-out/` は無視しない）
- Create: `PHASE`（一行 `P0`）
- Create: `README.md`（SPEC へのポインタとビルド 3 行）

## Interfaces

- Consumes: なし
- Produces: git リポジトリ、`PHASE=P0`

## テスト

レイアウト。`test -f PHASE && test "$(cat PHASE)" = P0`

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

`git init -b main`。Intel / 長い README / GitHub remote は作らない。

## コミット文

```
Add repository legal files and P0 marker

Graphify: n/a (graph not yet built)
Serena: n/a
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- git がある
