# P2-02: クラス骨格の割り当て

- 親フェーズ: [P2](../phases/P2.md)
- 状態: done
- 依存: P2-01

## 目的

クラスオブジェクトを未初期化で割り当て well-known に載せる。

## Files

- Create: `runtime/src/Bootstrap.cpp`
- Create: `runtime/include/ao/Bootstrap.hpp`

## Interfaces

- Consumes: WellKnown, Heap
- Produces: Object, Behavior, ClassDescription, Class, Metaclass とそのメタクラスの骨格 OOP。

## テスト

各骨格がヒープオブジェクトで、名前で引ける。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 リンクは P2-03。

## コミット文

```
Add class object skeletons

Graphify: path Bootstrap WellKnown
Serena: find_symbol ao::Bootstrap
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

