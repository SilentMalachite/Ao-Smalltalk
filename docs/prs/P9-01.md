# P9-01: Do it / Print it / Inspect it

- 親フェーズ: [P9](../phases/P9.md)
- 状態: done
- 依存: P8 完了

## 目的

選択範囲評価。Do it は結果捨て、Print it は printString 挿入、Inspect it は簡易オブジェクトビュー。self は nil。ワークスペース変数あり。

## Files

- Modify: `app/Ao/WorkspaceWindow.swift`
- Create: `app/AoTests/WorkspaceEvalTests.swift`

## Interfaces

- Consumes: 文字列ソース評価 ABI
- Produces: 3 コマンド。

## テスト

Do it / Print it XCTest。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add Do it Print it Inspect it

Graphify: path Workspace eval
Serena: find_symbol doIt
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

