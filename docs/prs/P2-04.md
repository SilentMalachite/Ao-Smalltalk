# P2-04: Smalltalk グローバル辞書

- 親フェーズ: [P2](../phases/P2.md)
- 状態: planned
- 依存: P2-03

## 目的

クラス名 → クラスを `Smalltalk` に登録する。

## Files

- Create: `runtime/include/ao/Globals.hpp`
- Create: `runtime/src/Globals.cpp`
- Modify: `runtime/tests/bootstrap_test.cpp`

## Interfaces

- Consumes: Bootstrap
- Produces: グローバル辞書 OOP。スナップショットは P7。

## テスト

名前 `Object` でクラス OOP が返る。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Register classes in Smalltalk dictionary

Graphify: path Smalltalk Class
Serena: find_symbol ao::Globals
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

