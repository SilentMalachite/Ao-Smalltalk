# P1-04: old 世代 mark-compact と昇格

- 親フェーズ: [P1](../phases/P1.md)
- 状態: planned
- 依存: P1-03

## 目的

生存オブジェクトの昇格と old mark-compact。内部ポインタ更新。

## Files

- Modify: `runtime/src/Gc.cpp`, `runtime/include/ao/Gc.hpp`
- Modify: `runtime/tests/gc_test.cpp`

## Interfaces

- Consumes: nursery GC
- Produces: `Gc::collectOld()`, 昇格。

## テスト

`gc_test`: 旧世代昇格、内部ポインタ更新。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add old-generation mark-compact

Graphify: path Gc Heap
Serena: find_referencing_symbols ao::Gc
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

