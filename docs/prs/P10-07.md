# P10-07: 受け入れ、CHANGELOG、Graphify deep

- 親フェーズ: [P10](../phases/P10.md)
- 状態: done
- 依存: P10-06

## 目的

P10 の受け入れを閉じる。SPEC §6 の「デバッガ」小節と `docs/phases/P10.md` を `[x]` にし、CHANGELOG に項目を足し、グラフを deep で作り直す。

## Files

- Modify: `SPEC.md`（§6「デバッガ」）
- Modify: `docs/phases/P10.md`（受け入れ）
- Modify: `CHANGELOG.md`（`[Unreleased]` に Runtime / Compiler / Ao.app の項目。Known limitations の「No debugger …」を「No live debugger (Proceed / Step; P11)」に直す）
- Modify: `graphify-out/`（`graphify . --mode deep`）

## Interfaces

- Consumes: P10-02 … P10-06 の緑
- Produces: 閉じたチェックリスト、CHANGELOG、deep グラフ

## テスト

`scripts/test.sh`、`AO_GC_STRESS=1` の `gcstress` / `gcstress_vendor`、`build/ao --test image/tests`、`scripts/test.sh --app`。手動確認（`scripts/package-app.sh` の `Ao.app`）:

1. `nil foo` の Do it → Debug → 最内 `#foo (doesNotUnderstand:)`、次が doIt で `nil foo` が選択される
2. `self halt` → 理由 `halt`
3. `#(1 2) at: 5` → 最内 `ArrayedCollection>>at: native …`、doIt で送信全体の `#(1 2) at: 5` が選択される
4. `#(1 2) do: [:e | e foo]` → `[] in` フレームの variables に `e`。ダブルクリックで Inspector
5. Browser で Accept したメソッドの中の失敗 → そのソースで送信が選択される。vendor メソッドの中の失敗 → プレースホルダ
6. 次の Do it で開いていた Debugger の Inspect が何もしない
7. `ao --test image/tests` と CLI が変わらない

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、上のテストと手動確認をすべて通してからチェックを付ける。確認した日付をフェーズ文書に書く（P9 と同じ）。

P1 以降のコードをこの PR 文書の範囲外で書かない。P11 に進まない（`PHASE` は `P10` のまま。P11 の PR 指示書は別セッションで書く）。

## コミット文

```
Close the P10 acceptance checklist and rebuild the knowledge graph

Graphify: . --mode deep
Serena: (docs only)
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる（§6）
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- `graphify . --mode deep` 済み
