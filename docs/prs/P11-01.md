# P11-01: SPEC の詳細化、PR 指示書、PHASE、ABI の定数

- 親フェーズ: [P11](../phases/P11.md)
- 状態: done
- 依存: P10 完了（`b89dde7`）

## 目的

SPEC §3.13 の「P11 ライブデバッガの設計判断」を、実装できる詳細に書き直す。コードより先に仕様を直す（SPEC §8）。計画書 [`p11-ancient-matsumoto.md`](../plans/p11-ancient-matsumoto.md) の「追加の設計判断」と、ユーザーの決定（`NonBoolean receiver` と `cannot return` は止めるが Proceed / Step は無効。止まったプロセスの窓を閉じたら Abort）を入れる。

## Files

- Modify: `SPEC.md`（§3.3、§3.4、§3.8 の文の先頭表、§3.9 の Workspace とライブ Debugger とメニュー、§3.10 の busy と `AO_ERR_HALT` と「ライブデバッガの操作」、§3.11 の `halted processes`、§3.13 の「ライブデバッガ」、§4.1–§4.3 のテスト名、§6 の「ライブデバッガ」）
- Modify: `PHASE`（`P11`）
- Modify: `docs/phases/P11.md`（正本の場所、受け入れ、PR 一覧）、`docs/README.md`（状態と PR 表）
- Create: `docs/prs/P11-01.md` … `P11-07.md`
- Modify: `bridge/ao_abi.h`（`AO_ERR_HALT`、`AO_EVAL_DEBUGIT`、`AO_DEBUG_POSTMORTEM` / `AO_DEBUG_LIVE` の定数だけ）

## Interfaces

- Consumes: SPEC §3.13 の P11 の設計判断、P10 の `DebugFrames` / `DebugSnapshot` / `ao_debug_*`
- Produces: §3.13「ライブデバッガ」（評価プロセス、止める表、止まったプロセスのフレーム、操作）、§3.10「ライブデバッガの操作」（関数の契約）、§6 の受け入れ 11 項目。以降の PR はこの契約で書く

## テスト

なし（docs と定数）。ヘッダを足したあと `scripts/test.sh` のビルドが通ること。

## 手順

`graphify-out/GRAPH_REPORT.md` を読み、`graphify path Scheduler Session --undirected`、`graphify explain resumeTo`、`graphify explain DebugFrames`、`graphify path Interpreter abortEvaluation --undirected` で経路を取る。Serena で `Scheduler::terminate`、`runFiber`、`afterResume`、`abortEvaluation`、`truthOf`、`ao_Object_error_`、`ao_BlockContext_cannotReturn_`、`Codegen::mark` の定義と参照を見て、SPEC の記述とずれないことを確かめる。

## コミット文

```
Detail the live debugger in the spec and write the P11 PR docs

Graphify: explain DebugFrames; path Scheduler Session
Serena: find_referencing_symbols ao::abortEvaluation
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる（この PR が含める）
- ビルドが通る
- 頼まれていないファイルを増やしていない
- `graphify update .` 済み
