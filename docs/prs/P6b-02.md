# P6b-02: LOAD_ORDER と Kernel 上書き禁止

- 親フェーズ: [P6b](../phases/P6b.md)
- 状態: done
- 依存: P6b-01

## 目的

ロード順固定。NativeMethod があるセレクタは上書き禁止。

## Files

- Create: `image/vendor/LOAD_ORDER`
- Create: `runtime/tests/vendor_overwrite_test.cpp`

## Interfaces

- Consumes: Kernel 走査
- Produces: 上書き禁止チェック。

## テスト

vendor 由来がネイティブ必須セレクタを上書きしていない。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add vendor load order and overwrite guard

Graphify: path LOAD_ORDER NativeMethod
Serena: find_symbol kernel scan
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

