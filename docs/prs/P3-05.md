# P3-05: インラインキャッシュとクラスキャッシュ

- 親フェーズ: [P3](../phases/P3.md)
- 状態: done
- 依存: P3-04

## 目的

通常パス: IC 1 段 → クラス側メガモーフィックキャッシュ → 辞書。

## Files

- Create: `runtime/include/ao/Send.hpp`
- Create: `runtime/src/Send.cpp`
- Create: `runtime/tests/native_send_test.cpp`

## Interfaces

- Consumes: lookup
- Produces: `send(receiver, selector, args) -> Oop`

## テスト

`native_send_test`: `#==`、経路証明用スタブ `SmallInteger>>+`（P4-03 で本物に置換すると明記）。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add native send caches

Graphify: path send lookup
Serena: find_symbol ao::send
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

