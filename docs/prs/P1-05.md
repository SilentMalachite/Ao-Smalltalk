# P1-05: GC ルート API

- 親フェーズ: [P1](../phases/P1.md)
- 状態: planned
- 依存: P1-04

## 目的

グローバル辞書スロット、well-known 配列、ホストハンドル表、スタック OOP 報告のインタフェース。

## Files

- Create: `runtime/include/ao/Roots.hpp`
- Create: `runtime/src/Roots.cpp`
- Modify: `runtime/tests/gc_test.cpp`

## Interfaces

- Consumes: Gc
- Produces: `Roots::add(Oop*)`, ハンドル表（中身空）、スタックウォークのフック。

## テスト

ルートに載せたオブジェクトが collect 後も生きる。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 AppKit ハンドルの中身は空。well-known 表のクラス意味論は P2。

## コミット文

```
Add GC root API

Graphify: path Roots Gc
Serena: find_symbol ao::Roots
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

