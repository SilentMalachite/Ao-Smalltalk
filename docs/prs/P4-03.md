# P4-03: Magnitude / SmallInteger / Character

- 親フェーズ: [P4](../phases/P4.md)
- 状態: planned
- 依存: 先行 P4 PR

## 目的

算術はネイティブ。オーバーフローは LargeInteger へ透過。P3 のスタブ `+` を置換。

## Files

- Create: `runtime/src/kernel/SmallInteger.cpp`
- Create: `runtime/tests/smallinteger_arith_test.cpp`

## Interfaces

- Consumes: send / NativeMethod / Bootstrap
- Produces: `+ - * // \\ quo: rem: bitAnd: bitOr: bitXor: bitShift: = < > <= >=`

## テスト

`smallinteger_arith_test`

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 Smalltalk ソースを実行定義にしない。

## コミット文

```
Add SmallInteger arithmetic

Graphify: path NativeMethod Magnitude
Serena: find_symbol ao_Magnitude
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- Kernel を触るので走査テストがあるなら緑（P4-09 以降必須）
