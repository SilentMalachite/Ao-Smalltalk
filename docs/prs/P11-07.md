# P11-07: 受け入れ、CHANGELOG、Graphify deep

- 親フェーズ: [P11](../phases/P11.md)
- 状態: done
- 依存: P11-06

## 目的

P11 の受け入れを閉じる。自動テストで確かめた項目を SPEC §6「ライブデバッガ」と `docs/phases/P11.md` で `[x]` にし、CHANGELOG に項目を足し、グラフを deep で作り直す。

## Files

- Modify: `SPEC.md`（§6「ライブデバッガ」）
- Modify: `docs/phases/P11.md`（受け入れと手動確認）
- Modify: `CHANGELOG.md`（`[Unreleased]` に Runtime / Compiler / Ao.app の項目。Known limitations の「No live debugger」を消す）
- Modify: `graphify-out/`（`graphify . --mode deep`）

## Interfaces

- Consumes: P11-02 … P11-06 の緑
- Produces: 閉じたチェックリスト、CHANGELOG、deep グラフ

## テスト

`scripts/test.sh`、`AO_GC_STRESS=1` の `gcstress` / `gcstress_vendor`、`build/ao --test image/tests`、`scripts/test.sh --app`。`ao --test image/tests`、`ao --version`、`ao filein` の出力と終了コードが P10 の HEAD と同じ。手動確認（`scripts/package-app.sh` の `Ao.app`）:

1. `self halt` から Proceed し、Print it の結果が出る
2. `[self error: 'x'] ensure: [Transcript show: 'done']` で Abort し、Transcript に `done` が出る
3. Step over / Step into / Step out で選択が動く
4. `3 ifTrue: [4]` で Proceed が無効になっている
5. Debugger を開いたまま、Workspace で別の Do it ができる
6. 止まったまま Save Image を選ぶと、`halted processes` の警告が出る
7. Debug it（`⌘⇧D`）で最初の文で止まる
8. 窓を閉じると Abort される

手で操作できない項目は、フェーズ文書の「手動確認」に未確認として残す。

## 手順

`graphify-out/GRAPH_REPORT.md` を読み、上のテストを通してからチェックを付ける。確認した日付をフェーズ文書に書く。

## コミット文

```
Close the P11 acceptance checklist and rebuild the knowledge graph

Graphify: . --mode deep
Serena: (docs only)
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる（§6）
- 新規または更新したテストが緑
- 頼まれていないファイルを増やしていない
- `graphify . --mode deep` 済み
