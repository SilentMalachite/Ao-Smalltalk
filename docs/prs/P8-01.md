# P8-01: Ao.app 骨格

- 親フェーズ: [P8](../phases/P8.md)
- 状態: planned
- 依存: P7 完了

## 目的

macOS アプリ。独立 NSWindow。バンドル ID `jp.ao.smalltalk`。

## Files

- Create: Xcode プロジェクトまたは app ターゲット（SPEC `app/Ao/`）
- Create: `app/Ao/AppDelegate.swift`
- Modify: `bridge/ao_abi.h`（起動/終了。SPEC を先に更新してから増やす）

## Interfaces

- Consumes: ao_runtime_boot / shutdown
- Produces: 起動するアプリ（ツール窓は後続）。

## テスト

XCTest で起動ヘルパが ABI boot を呼ぶ。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 SwiftUI 主系統にしない。C++ テンプレートに Swift が依存しない。

## コミット文

```
Add Ao.app skeleton

Graphify: path AoApp ao_abi
Serena: get_symbols_overview app/Ao
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

