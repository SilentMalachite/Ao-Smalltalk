# P10-01: SPEC と CLAUDE.md の改訂、PHASE

- 親フェーズ: [P10](../phases/P10.md)
- 状態: done
- 依存: P9 完了（v1 1.0.0）

## 目的

デバッガを SPEC の範囲に入れる。コードより先に仕様を直す（SPEC §8、CONTRIBUTING.ja.md）。計画書 [`2026-09-26-p10-debugger.md`](../superpowers/plans/2026-09-26-p10-debugger.md) の「SPEC を先に直す」の差分表を適用する。

## Files

- Modify: `SPEC.md`（§1.4、§2.3、§3.3、§3.4、§3.5、§3.6、§3.8、§3.9、§3.10、§4.1、§4.3、§5.4、§6。§3.13「デバッガ」を新設）
- Modify: `CLAUDE.md`（禁止リストから「デバッガ」を外す）
- Modify: `PHASE`（`P10`）
- Modify: `docs/README.md`（表の確認。計画と同時に追記済み）

## Interfaces

- Consumes: 計画書の設計判断（フレーム連鎖、pc→ソース表、捕捉、`halt`、`ao_debug_*`、Debugger 窓、P11 の設計）
- Produces: §3.13 を正本とする捕捉の意味論。§3.10 の `ao_debug_*` の契約。§6 の「デバッガ」小節（受け入れ 9 項目。P10 のフェーズ文書と同じ）。§3.13 の末尾に P11 の設計判断（評価プロセス、`AO_ERR_HALT`、Proceed / Abort / Step、`halted processes`）

## テスト

なし（docs のみ）。SPEC の節番号の相互参照（§3.4 → §3.13、§3.9 → §3.10、§4.1 のテスト名）を目視で確認する。`grep -n "デバッガ" SPEC.md CLAUDE.md` で範囲外の記述が残っていないことを確かめる。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、`graphify query "SPEC 3.10 ao_eval AoSpan"` で §3.10 と ABI の結びつきを見てから書く。SPEC の既存の文体（である調、規則を箇条書き、理由の文言はバッククォート）に合わせる。§3.13 の P11 の部分は「P11 で実装する。P10 では捕捉だけ」と明記する。

P1 以降のコードをこの PR 文書の範囲外で書かない。この PR ではコードを書かない。

## コミット文

```
Bring the debugger into the spec as P10 and P11

Graphify: query SPEC 3.10 ao_eval AoSpan
Serena: (docs only)
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる（この PR が含める）
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- `graphify update .` 済み
