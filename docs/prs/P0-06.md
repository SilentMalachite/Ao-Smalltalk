# P0-06: Serena プロジェクト

- 親フェーズ: [P0](../phases/P0.md)
- 状態: done in P0 execution
- 依存: P0-03

## 目的

clangd / SourceKit が解決できる Serena プロジェクトと Grok の project MCP。

## Files

- Create: `.serena/project.yml`
- Create: `.serena/memories/{bootstrap_order,abi_boundary,kernel_is_native}.md`
- Create: `.grok/config.toml`（Serena MCP、`--project-from-cwd`）

## Interfaces

- Consumes: `scripts/build.sh` が作る `compile_commands.json`（gitignore）
- Produces: 次セッションで `activate_project` 可能な状態

## テスト

`test -f .serena/project.yml`; `grok mcp list` に serena。このセッションで `find_symbol` が使えないことは既知。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

生成された project.yml を正とする。絶対パスを config に書かない。秘密を書かない。

## コミット文

```
Add Serena project metadata and project MCP

Graphify: n/a
Serena: project.yml created; activate next session
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

