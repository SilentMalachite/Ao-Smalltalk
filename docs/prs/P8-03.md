# P8-03: Workspace ウィンドウ

- 親フェーズ: [P8](../phases/P8.md)
- 状態: planned
- 依存: P8-01

## 目的

自由テキスト。評価コマンドの配線は P9。P8 は窓とメニュー項目の受け皿。

## Files

- Create: `app/Ao/WorkspaceWindow.swift`

## Interfaces

- Consumes: NSTextView
- Produces: 起動時 Workspace。

## テスト

窓が作れる。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add Workspace window

Graphify: query WorkspaceWindow
Serena: find_symbol WorkspaceWindow
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

