# P6b-04: vendor file-in

- 親フェーズ: [P6b](../phases/P6b.md)
- 状態: done
- 依存: P6b-03

## 目的

LOAD_ORDER どおり CompiledMethod として載せる。

## Files

- Modify: `runtime/src/ao_main.cpp`（`ao filein`）
- Create: `runtime/tests/vendor_filein_test.cpp`

## Interfaces

- Consumes: Chunk parser, Interpreter
- Produces: 非 Kernel メソッドが評価できる。

## テスト

file-in した非 Kernel が CompiledMethod として評価できる（SPEC §6）。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
File-in vendor sources

Graphify: path filein CompiledMethod
Serena: find_symbol filein
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

