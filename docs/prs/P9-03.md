# P9-03: エラー表示と VoiceOver

- 親フェーズ: [P9](../phases/P9.md)
- 状態: planned
- 依存: P9-02

## 目的

エラーオブジェクトの文字列化。主要コントロールに VO ラベル。過度なアニメーションなし。階層表示。

## Files

- Modify: Browser / Workspace
- Create: `app/AoTests/ErrorDisplayTests.swift`

## Interfaces

- Consumes: エラー OOP
- Produces: 区間付きエラー表示、hierarchy コマンドまたは代替。

## テスト

失敗 accept がテキストを壊さない。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add error display and accessibility labels

Graphify: path error Browser
Serena: find_symbol presentError
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

