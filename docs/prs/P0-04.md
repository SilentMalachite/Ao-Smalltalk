# P0-04: C ABI + Swift 空テスト

- 親フェーズ: [P0](../phases/P0.md)
- 状態: done in P0 execution
- 依存: P0-03

## 目的

C ABI の最小と Swift 6 空 XCTest。GUI は作らない。

## Files

- Create: `bridge/ao_abi.h`, `runtime/src/abi.cpp`
- Create: `app/Package.swift`, `app/Ao/Placeholder.swift`, `app/AoTests/SmokeTests.swift`
- Modify: `runtime/CMakeLists.txt`, `scripts/test.sh`

## Interfaces

- Consumes: `ao::boot` / `shutdown` / `version_string`
- Produces: `ao_version`, `ao_runtime_boot`, `ao_runtime_shutdown`; `AO_OK`/`AO_ERR`; `AoPlaceholder.phase`

## テスト

`AbiSmoke.BootVersionShutdown`（ヘッダ無しでコンパイル失敗を先に見る）、`SmokeTests.testPlaceholderTypeExists`。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

評価 API を宣言しない。Swift は ABI をリンクしない（P8）。

## コミット文

```
Add C ABI stub and Swift smoke tests

Graphify: n/a
Serena: n/a
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- `./scripts/test.sh` が ctest と swift test の両方緑
