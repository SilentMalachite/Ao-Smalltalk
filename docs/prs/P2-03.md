# P2-03: メタクラス循環（Blue Book 6–10）

- 親フェーズ: [P2](../phases/P2.md)
- 状態: done
- 依存: P2-02

## 目的

superclass / methodDict / format / メタクラスリンクを結ぶ。

## Files

- Modify: `runtime/src/Bootstrap.cpp`
- Modify: `runtime/tests/bootstrap_test.cpp`

## Interfaces

- Consumes: クラス骨格
- Produces: 循環不変条件を満たすリンク。`Object superclass` は nil。

## テスト

`bootstrap_test` 10 項目。SPEC §6: `Object class class == Metaclass`, `Metaclass class class == Metaclass`。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 送信は実装しない。

## コミット文

```
Wire metaclass cycle

Graphify: path Object Metaclass
Serena: find_referencing_symbols ao::Bootstrap
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

