# P4-09: Kernel NativeMethod 走査と bench

- 親フェーズ: [P4](../phases/P4.md)
- 状態: done
- 依存: 先行 P4 PR

## 目的

辞書の値がすべて NativeMethod。マイクロベンチを記録。

## Files

- Create: `runtime/tests/kernel_scan_test.cpp`
- Create: `docs/bench.md`

## Interfaces

- Consumes: send / NativeMethod / Bootstrap
- Produces: 走査 API

## テスト

Kernel 走査テスト。`docs/bench.md` に `1 to: 10000000` 相当。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 Smalltalk ソースを実行定義にしない。

## コミット文

```
Add Kernel NativeMethod scan and bench

Graphify: path NativeMethod Kernel
Serena: find_symbol ao_Kernel
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- Kernel を触るので走査テストがあるなら緑（P4-09 以降必須）
