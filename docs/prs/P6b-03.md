# P6b-03: host 移設パッチ

- 親フェーズ: [P6b](../phases/P6b.md)
- 状態: done
- 依存: P6b-02

## 目的

描画・ファイルダイアログ・Browser ビュー参照をスタブまたは削除。

## Files

- Create: `image/patches/` の最小差分

## Interfaces

- Consumes: vendor ソース
- Produces: Ao 向け最小パッチ。自作ライブラリは禁止。

## テスト

パッチが Morphic/BitBlt を呼び出さない。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add vendor host patches

Graphify: query image/patches
Serena: n/a
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

