# P6-01: MethodContext / BlockContext

- 親フェーズ: [P6](../phases/P6.md)
- 状態: planned
- 依存: P5 完了

## 目的

ユーザーメソッドとブロックのコンテキスト。SPEC §3.4。

## Files

- Create: `runtime/include/ao/Context.hpp`
- Create: `runtime/src/Context.cpp`
- Create: `runtime/tests/context_test.cpp`

## Interfaces

- Consumes: CompiledMethod, Heap
- Produces: MethodContext / BlockContext（または統一 Context + フラグ）。外側コンテキストとコピー値。

## テスト

コンテキストが GC ルートとして報告される。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add method and block contexts

Graphify: path Context Gc
Serena: find_symbol ao::Context
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

