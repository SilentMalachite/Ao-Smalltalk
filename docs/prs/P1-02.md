# P1-02: オブジェクトヘッダと bump 割り当て

- 親フェーズ: [P1](../phases/P1.md)
- 状態: done
- 依存: P1-01

## 目的

ヘッダ付きオブジェクトを nursery bump で割り当てる。

## Files

- Create: `runtime/include/ao/Heap.hpp`
- Create: `runtime/src/Heap.cpp`
- Create: `runtime/tests/heap_alloc_test.cpp`

## Interfaces

- Consumes: `ao::Oop`
- Produces: `Heap::allocate(Oop cls, uint32_t size, uint16_t flags) -> Oop`。ヘッダ: class 64, size 32, flags 16, hash 16。

## テスト

`heap_alloc_test`: 割り当て、サイズ、クラススロット。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 可変長ペイロードはヘッダ直後。GC はまだ動かない。

## コミット文

```
Add heap bump allocator

Graphify: path Heap Oop
Serena: find_symbol ao::Heap::allocate
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

