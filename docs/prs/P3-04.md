# P3-04: lookup / super / doesNotUnderstand:

- 親フェーズ: [P3](../phases/P3.md)
- 状態: done
- 依存: P3-03

## 目的

Blue Book の探索意味論。

## Files

- Create: `runtime/include/ao/Lookup.hpp`
- Create: `runtime/src/Lookup.cpp`
- Create: `runtime/tests/lookup_test.cpp`

## Interfaces

- Consumes: クラス階層、MethodDictionary、NativeMethod
- Produces: `lookup(receiver, selector) -> method`。super は定義クラスのスーパークラスから。失敗は DNU。

## テスト

`lookup_test`: 継承、super、doesNotUnderstand。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 Object の DNU / == / class は経路に必要な最小 NativeMethod だけ。充足は P4。

## コミット文

```
Add method lookup

Graphify: path lookup MethodDictionary
Serena: find_symbol ao::lookup
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

