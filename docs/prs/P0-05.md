# P0-05: docs/phases と docs/prs

- 親フェーズ: [P0](../phases/P0.md)
- 状態: in progress
- 依存: P0-02

## 目的

v1 全フェーズと全 PR の実行文書を残す。P1 以降のコードは書かない。

## Files

- Create: `docs/README.md`
- Create: `docs/phases/P0.md` … `P9.md`, `P6b.md`
- Create: `docs/prs/*.md`（本索引の 48 ファイル）

## Interfaces

- Consumes: SPEC §2.3 §3 §4 §6
- Produces: 以降フェーズの実行正本（計画文書）

## テスト

`test -f docs/phases/P9.md && test -f docs/prs/P9-04.md`。SPEC §4 スイート名がいずれかの PR に現れること。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

SPEC 本文をコピーしない。節番号で参照。`docs/native-selectors.md` と `docs/bench.md` は作らない。

## コミット文

```
Add phase and PR implementation documents

Graphify: n/a (P0-07)
Serena: n/a
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

