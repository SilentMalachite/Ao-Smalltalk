# P7-03: load と NativeMethod 再結合

- 親フェーズ: [P7](../phases/P7.md)
- 状態: planned
- 依存: P7-02

## 目的

再配置し、シンボル名で関数ポインタを結び直す。

## Files

- Create: `runtime/src/ImageLoad.cpp`
- Create: `runtime/tests/image_save_load_test.cpp`

## Interfaces

- Consumes: save
- Produces: `ao image load <path>`

## テスト

`image_save_load_test`: save 後に同一評価結果。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add aoimage load and native rebind

Graphify: path ImageLoad NativeMethod
Serena: find_symbol ao::loadImage
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

