# P7-02: save

- 親フェーズ: [P7](../phases/P7.md)
- 状態: planned
- 依存: P7-01

## 目的

well-known、ヒープ（オフセット再配置）、グローバル辞書を書く。

## Files

- Create: `runtime/src/ImageSave.cpp`
- Modify: `runtime/src/ao_main.cpp`

## Interfaces

- Consumes: ImageFormat, Heap, WellKnown, Globals
- Produces: `ao image save <path>`

## テスト

ファイルが AOIM で始まる。NativeMethod はシンボル名文字列。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add aoimage save

Graphify: path ImageSave NativeMethod
Serena: find_symbol ao::saveImage
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

