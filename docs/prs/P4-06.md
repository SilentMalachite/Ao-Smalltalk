# P4-06: Stream / Transcript モデル

- 親フェーズ: [P4](../phases/P4.md)
- 状態: planned
- 依存: 先行 P4 PR

## 目的

実際の出力先はホスト。P8 がウィンドウ。ここではコールバック。

## Files

- Create: `runtime/src/kernel/Stream.cpp`
- Create: `runtime/tests/transcript_model_test.cpp`

## Interfaces

- Consumes: send / NativeMethod / Bootstrap
- Produces: `Transcript show: / cr / nextPutAll: / clear` がコールバックを呼ぶ

## テスト

`transcript_model_test`

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 Smalltalk ソースを実行定義にしない。

## コミット文

```
Add Transcript model callbacks

Graphify: path NativeMethod Stream
Serena: find_symbol ao_Stream
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- Kernel を触るので走査テストがあるなら緑（P4-09 以降必須）
