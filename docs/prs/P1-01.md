# P1-01: ao::Oop タグ

- 親フェーズ: [P1](../phases/P1.md)
- 状態: done
- 依存: P0 完了

## 目的

64-bit tagged pointer を `ao::Oop` に閉じる。

## Files

- Create: `runtime/include/ao/Oop.hpp`
- Create: `runtime/src/Oop.cpp`
- Create: `runtime/tests/oop_tag_test.cpp`

## Interfaces

- Consumes: なし（値型）
- Produces: `Oop::fromSmallInteger(int64_t)`, `isSmallInteger()`, `smallIntegerValue()`; Character / immediate / heap 同様。`raw` は private。

## テスト

`oop_tag_test`: SmallInteger / Character / 即値 / ヒープのタグ往復。即値 3 パターン。ヒープは 8 バイト整列。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add tagged OOP tests

Graphify: query tagged pointer Oop
Serena: find_symbol ao::Oop
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

