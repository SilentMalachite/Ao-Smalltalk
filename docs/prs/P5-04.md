# P5-04: コード生成 CompiledMethod

- 親フェーズ: [P5](../phases/P5.md)
- 状態: planned
- 依存: P5-03

## 目的

AST → バイトコード + リテラル枠 + 一時変数数。nativeCode は nil。

## Files

- Create: `compiler/src/Codegen.cpp`
- Create: `compiler/tests/codegen_snapshot_test.cpp`

## Interfaces

- Consumes: P4 Kernel オブジェクト（CompiledMethod クラス）
- Produces: CompiledMethod OOP（実行しない）

## テスト

スナップショットテスト（安定したら固定）。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 インタプリタで走らせない。

## コミット文

```
Add bytecode codegen

Graphify: path compiler コード生成 CompiledMethod
Serena: get_symbols_overview compiler/src
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

