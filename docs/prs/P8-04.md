# P8-04: System Browser 5 ペイン

- 親フェーズ: [P8](../phases/P8.md)
- 状態: planned
- 依存: P8-01

## 目的

クラシック 5 ペイン契約を AppKit で。モデル列挙。accept は P9。

## Files

- Create: `app/Ao/BrowserWindow.swift`
- Create: `app/AoTests/BrowserModelTests.swift`

## Interfaces

- Consumes: ABI のクラス一覧・セレクタ一覧・ソース取得（SPEC 更新後に追加）
- Produces: 5 ペイン UI。Object のメソッド一覧が見えること（データが来れば）。

## テスト

Browser モデルのカテゴリ / クラス / セレクタ列挙。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 NSSplitView / NSTableView / NSTextView。白黒ビットマップを再現しない。

## コミット文

```
Add System Browser panes

Graphify: path BrowserWindow ao_abi
Serena: find_symbol BrowserWindow
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

