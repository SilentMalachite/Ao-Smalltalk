# P7-01: .aoimage 形式

- 親フェーズ: [P7](../phases/P7.md)
- 状態: done
- 依存: P6b 完了

## 目的

マジック AOIM、バージョン、ポインタサイズ、エンディアン、セクション配置。

## Files

- Create: `runtime/include/ao/ImageFormat.hpp`
- Create: `runtime/src/ImageFormat.cpp`

## Interfaces

- Consumes: Heap layout
- Produces: ヘッダ定数と読み書きの骨格（中身のオブジェクトは次 PR）。

## テスト

マジックとバージョンを書いたバッファを読める。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 関数ポインタ欄を設けない。

## コミット文

```
Add aoimage format header

Graphify: query AOIM
Serena: find_symbol ao::ImageFormat
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

