# P1-07: immovable old objects

- 親フェーズ: [P1](../phases/P1.md)
- 状態: done
- 依存: P1-06

## 目的

旧世代の `kFlagImmovable` オブジェクトは mark-compact でアドレスを変えない。

## Files

- Modify: `runtime/src/Gc.cpp`, `runtime/tests/gc_test.cpp`

## Interfaces

- Consumes: `collectOld` forwarding map
- Produces: pin-stable compact. nursery 昇格での移動は許可。

## テスト

`GcOld.Immovable*`

## 手順

Serena で `collectOld` の参照を取り、テストを赤にしてから forwarding だけ変える。free-list は足さない。
