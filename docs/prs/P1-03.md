# P1-03: nursery GC

- 親フェーズ: [P1](../phases/P1.md)
- 状態: planned
- 依存: P1-02

## 目的

nursery の正確 scavenge。

## Files

- Create: `runtime/include/ao/Gc.hpp`
- Create: `runtime/src/Gc.cpp`
- Create: `runtime/tests/gc_test.cpp`（nursery ケース）

## Interfaces

- Consumes: Heap, Oop, ルート登録インタフェース（最小）
- Produces: `Gc::collectNursery()`。ルートから見えるオブジェクトは生存。

## テスト

`gc_test`: nursery 回収、ルート保護。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 旧世代はまだ空でよい。

## コミット文

```
Add nursery GC

Graphify: path Gc Heap
Serena: find_symbol ao::Gc::collectNursery
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

