# P0-02: ディレクトリ骨格

- 親フェーズ: [P0](../phases/P0.md)
- 状態: done in P0 execution
- 依存: P0-01

## 目的

SPEC §2.2 のディレクトリ契約を空で満たす。

## Files

- Create: `runtime/`, `compiler/`, `bridge/`, `app/`, `image/*`, `docs/phases`, `docs/prs`, `scripts/`
- Create: `image/*/`.gitkeep

## Interfaces

- Consumes: P0-01
- Produces: SPEC §2.2 ディレクトリ

## テスト

`test -d runtime && test -d compiler && test -d image/Kernel`

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

空の C++/Swift ソースはまだ置かない（P0-03）。

## コミット文

```
Add SPEC directory skeleton

Graphify: n/a
Serena: n/a
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

