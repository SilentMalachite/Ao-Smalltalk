# P3-01: Symbol intern

- 親フェーズ: [P3](../phases/P3.md)
- 状態: done
- 依存: P2 完了

## 目的

同一文字列の Symbol は intern され `#==` が成り立つ。

## Files

- Create: `runtime/include/ao/Symbol.hpp`
- Create: `runtime/src/Symbol.cpp`
- Create: `runtime/tests/symbol_test.cpp`

## Interfaces

- Consumes: Heap, Oop
- Produces: `Symbol::intern(std::string_view) -> Oop`

## テスト

同じ内容はビット列が等しい。違う内容は等しくない。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add Symbol intern

Graphify: query Symbol intern
Serena: find_symbol ao::Symbol::intern
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

