# P10-05: ao_debug_* ABI

- 親フェーズ: [P10](../phases/P10.md)
- 状態: done
- 依存: P10-04

## 目的

ホストがスナップショットを読む C ABI。SPEC §3.10（P10-01 で改訂）の契約どおり。読みは busy でも呼べ、printString / inspect / clear は最外の入口で busy なら拒む。

## Files

- Modify: `bridge/ao_abi.h`（`ao_set_debug_capture`、`ao_debug_generation` / `frame_count` / `frame_total` / `reason` / `frame_kind` / `frame_label` / `frame_pc` / `frame_source` / `frame_temp_count` / `frame_temp_name` / `frame_receiver_print` / `frame_temp_print` / `inspect` / `clear`。31–38 行の busy コメントに追記）
- Modify: `runtime/src/abi.cpp`（読みは `guarded` のみ、4 つは `AbiEntry` + `guarded`。`g_debugCapture` は `g_transcriptFn` と同じく boot / load をまたいで残す）
- Modify: `runtime/src/Session.hpp`、`runtime/src/Session.cpp`（`debugFrameCount`、`debugFrameLabel`、`debugFrameSource`、`debugTempName`、`debugReceiverPrint`、`debugTempPrint`、`debugInspect`、`debugClear`、`setSessionDebugCapture`。inspect + `AoInspectFn` の列を `evalBody` から関数に切り出す）
- Create: `runtime/tests/debug_abi_test.cpp`（fixture `DebugAbi` は `failure_test.cpp` 25–48 行の `FailureAbort` に倣う）
- Modify: `runtime/CMakeLists.txt`

## Interfaces

- Consumes: `DebugSnapshot` / `DebugFrames`（P10-03）、`Session::debugInfoFor`（P10-04）、`sourceOf` のプレースホルダ、`writeBuf`、`loadedImageProbes` の送信手順
- Produces: SPEC §3.10 の `ao_debug_*`。ラベル `Foo>>bar` / `[] in Foo>>bar` / `ArrayedCollection>>at: native ao_ArrayedCollection_at_` / `#foo (doesNotUnderstand:)`。`frame_source` の `highlight`（UTF-8 バイト、doIt は前置分を引く）。デバッグ情報が無い temp 名は `arg1` / `t1`

## テスト

`debug_abi_test.cpp`（赤にしてから実装）:

- `DebugAbi.EvalErrorFillsFramesInnermostFirst`
- `DebugAbi.FrameSourceHighlightsFailingSend`
- `DebugAbi.DoItFrameSourceDropsPrefix`
- `DebugAbi.BlockFrameLabelIsBracketsIn`
- `DebugAbi.NativeFrameLabelNamesSymbol`
- `DebugAbi.TempPrintIsRefusedWhileBusy`（transcript フックからの再入。既存の busy テストと同じ形）
- `DebugAbi.TempPrintAbortDoesNotReplaceSnapshot`
- `DebugAbi.NextEvalClearsSnapshotAndBumpsGeneration`
- `DebugAbi.ClearDropsRoots`（ルートの数が元に戻る）
- `DebugAbi.CaptureOffLeavesNoFrames`
- `DebugAbi.NoSourceMethodAnswersPlaceholderAndGenericTempNames`
- `DebugAbi.InspectFiresHookWithTempValue`
- `DebugAbi.ProcessFailureIsReadableAfterEval`（`AO_OK` の drain 中のプロセスの失敗）
- `DebugAbi.BuffersFollowRangeRule`
- `DebugAbi.CaptureSettingSurvivesBootAndLoad`

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、`graphify query "ao_eval AbiEntry guarded writeBuf"` で経路を取り、Serena で `AbiEntry`、`guarded`、`sessionEval`、`sourceOf`、`browserSource`、`writeBuf`、`loadedImageProbes`、`setSessionTranscriptHook` の定義と参照を取ってからテストを赤にする。ヘッダのコメントは既存の英語の文体に合わせる。

P1 以降のコードをこの PR 文書の範囲外で書かない。Swift は P10-06。

## コミット文

```
Add the ao_debug_* C ABI over the abort snapshot

Graphify: query ao_eval AbiEntry guarded writeBuf
Serena: insert_after_symbol ao_eval_result_copy
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる（§3.10）
- 新規または更新したテストが緑。gcstress 緑
- 頼まれていないファイルを増やしていない
- `graphify update .` 済み
