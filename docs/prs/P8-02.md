# P8-02: Transcript ウィンドウ

- 親フェーズ: [P8](../phases/P8.md)
- 状態: planned
- 依存: P8-01

## 目的

show:/cr/nextPutAll:/clear がこの窓に出る。閉じてもオブジェクトは生きる。

## Files

- Create: `app/Ao/TranscriptWindow.swift`
- Modify: ABI Transcript コールバック（SPEC §3.10。未記載の詳細は SPEC 先）

## Interfaces

- Consumes: transcript_model_test のコールバック
- Produces: 起動時 1 枚の Transcript。

## テスト

コールバックがテキストを追加する（モデルまたは XCTest）。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add Transcript window

Graphify: path Transcript ao_abi
Serena: find_symbol TranscriptWindow
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

