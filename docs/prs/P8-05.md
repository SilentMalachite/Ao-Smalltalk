# P8-05: メニューとキー

- 親フェーズ: [P8](../phases/P8.md)
- 状態: planned
- 依存: P8-02, P8-03, P8-04

## 目的

Ao / File / Edit / Smalltalk / Tools / Window / Help。Do it ⌘D、Print it ⌘I。⌘P は Print it にしない。

## Files

- Create: `app/Ao/MainMenu.swift`

## Interfaces

- Consumes: 3 ツール窓
- Produces: Tools: Browser, Transcript, Workspace。Smalltalk: Do it, Print it, Inspect it, Accept。

## テスト

メニュー項目が存在する（XCTest または起動チェックリスト）。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add menus and keybindings

Graphify: query MainMenu
Serena: find_symbol MainMenu
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

