# P4-02: Behavior / ClassDescription / Class / Metaclass

- 親フェーズ: [P4](../phases/P4.md)
- 状態: done
- 依存: 先行 P4 PR

## 目的

SPEC §3.6 Kernel-Classes。new / basicNew / サブクラス作成セレクタ。

## Files

- Create: `runtime/src/kernel/Behavior.cpp`
- Create: `image/Kernel/Behavior.st（コメントのみ）`

## Interfaces

- Consumes: send / NativeMethod / Bootstrap
- Produces: Behavior 必須セレクタ

## テスト

`Object new class` が Object。Metaclass class new の禁止または制御。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。 Smalltalk ソースを実行定義にしない。

## コミット文

```
Add Behavior Class native methods

Graphify: path NativeMethod Behavior
Serena: find_symbol ao_Behavior
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない
- Kernel を触るので走査テストがあるなら緑（P4-09 以降必須）
