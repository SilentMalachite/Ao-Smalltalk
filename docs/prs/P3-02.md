# P3-02: MethodDictionary

- 親フェーズ: [P3](../phases/P3.md)
- 状態: planned
- 依存: P3-01

## 目的

Symbol キー identity のメソッド辞書。

## Files

- Create: `runtime/include/ao/MethodDictionary.hpp`
- Create: `runtime/src/MethodDictionary.cpp`
- Create: `runtime/tests/method_dictionary_test.cpp`

## Interfaces

- Consumes: Symbol
- Produces: `at:`, `at:put:` 相当の C++ API。

## テスト

同一 Symbol で入れた値が引ける。別 intern でない同一内容も intern 経由で当たる。

## 手順

この PR を実装するセッションは `graphify-out/GRAPH_REPORT.md` を読み、Serena で定義と参照を取ってからテストを赤にする。新規ファイル作成後は overview を取る。

P1 以降のコードをこの PR 文書の範囲外で書かない。 Kernel を触る PR は走査テストがあるフェーズ以降、それが緑であること。

## コミット文

```
Add MethodDictionary

Graphify: path MethodDictionary Symbol
Serena: find_symbol ao::MethodDictionary
```

## 完了条件

- 対象が `SPEC.md` のこのフェーズに含まれる
- 新規または更新したテストが緑（レイアウトのみの PR はコマンド検証）
- 頼まれていないファイルを増やしていない

