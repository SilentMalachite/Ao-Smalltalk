# P9 — 統合

## 結論

Do it / Print it / Inspect it、Browser accept、階層、VoiceOver、ゴールデン `#(1 2 3) collect:`。起動は 30 ms。比 5.76 は再計測していない。窓操作は 2026-09-26 に手動で確認した。SPEC §6 はすべて `[x]` で、v1 を 1.0.0 としてリリースした（SPEC §2.4）。

## 前提

P8 緑。

## 範囲

**やる:** 評価コマンド、Browser accept（メソッドとクラス定義）、エラーをテキスト破壊なしで表示、VoiceOver ラベル、ゴールデン受け入れ。

**やらない:** 次バージョンの JIT / FFI / デバッガ。

## 仕様

accept 失敗時にソースペインを壊さない（SPEC §3.8）。`#(1 2 3) collect: [:x | x * 2]` が `#(2 4 6)`。ユーザーが Browser から `Object>>foo` を追加し Workspace から評価できる。

## TDD

XCTest: accept 成功と失敗、Workspace Do it / Print it。`image/tests/*.st` ゴールデン。SPEC §6 チェックリスト。

## 制約

起動 2 秒以内が未達なら理由を SPEC に書く。性能ゲートは緩い（SPEC §6）。

## 受け入れ

SPEC §6 は緑のテストがある項目と、手動で確認した窓操作を `[x]`。CI（SPEC §4.5）がブランチ保護で必須。`PHASE` は `P9`。

- [x] 起動して閉じる
- [x] 最小化する
- [x] Spaces

## PR 一覧

[P9-01](../prs/P9-01.md) … [P9-04](../prs/P9-04.md)

## Graphify / Serena

このクローズは `graphify update .`。deep はしていない。Serena で `ao_Object_printString` が解決できる。
