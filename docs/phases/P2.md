# P2 — ブートストラップ

## 結論

Object〜Metaclass の循環と well-known 表を C++ で手書きする。メッセージ送信はまだ実装しない。

## 前提

P1 緑。SPEC §3.7。メタクラス規則は Blue Book 6–10。

## 範囲

**やる:** `WellKnown.hpp`、即値のクラス、未初期化クラス骨格、リンク、`Smalltalk` グローバル辞書。

**やらない:** NativeMethod インストール（P3）、file-in（P6b）。

## 仕様

順序は SPEC §3.7 の 1–6（7 のスナップショットは P7）。

`Object superclass` は `nil`。変えるなら SPEC §6 を先に更新する。

## TDD

`bootstrap_test`: メタクラス循環の不変条件 10 項目。SPEC §6 の `Object class class == Metaclass`、`Metaclass class class == Metaclass` を含む。

## 制約

循環を Smalltalk ソースから作らない。Kernel `.st` を実行定義にしない。

## 受け入れ

- [ ] well-known を名前でテストから参照できる
- [ ] Blue Book 6–10 が `bootstrap_test` で緑
- [ ] `Smalltalk` にクラス名 → クラスがある

## PR 一覧

[P2-01](../prs/P2-01.md) … [P2-04](../prs/P2-04.md)

## Graphify / Serena

経路: `WellKnown` → クラスオブジェクト → メタクラス。`Object` / `Behavior` 相当のシンボルを Serena で解決できること（SPEC §6 プロセス）。
