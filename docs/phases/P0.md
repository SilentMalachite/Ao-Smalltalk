# P0 — 骨格

## 結論

リポジトリ、CMake ビルド、空のテスト、Serena プロジェクト、Graphify 初期グラフ、本 `docs/` を出す。P1 以降のコードは書かない。

## 前提

- 製品: `SPEC.md` §2.2, §2.3 P0
- プロセス: `CLAUDE.md`（PHASE が無いときは P0）
- 対象: Apple Silicon、C++20、Swift 6

## 範囲

**やる:** ディレクトリ契約、`libao_runtime` / `libao_compiler` / `ao` CLI スタブ、`bridge/ao_abi.h` の version/boot/shutdown、Swift 空 XCTest、Serena、Graphify、フェーズ/PR 文書。

**やらない:** tagged OOP、GC、クラス循環、ディスパッチ、Kernel 充足、コンパイラ本体、GUI、vendor file-in、mimalloc。

## 仕様

- C++ は CMake 3.28 + Ninja。`CMAKE_EXPORT_COMPILE_COMMANDS=ON`。
- Swift は `app/Package.swift` が `app/Ao/` と `app/AoTests/` を指す。`Ao.app` は P8。
- ABI に評価・Browser・イメージ API をまだ宣言しない。

## TDD

- `runtime/tests/smoke_test.cpp` — boot / version / ABI
- `compiler/tests/smoke_test.cpp` — `ao::compiler::version()`
- `app/AoTests/SmokeTests.swift` — `AoPlaceholder.phase == "P0"`

タグ付けや `1 + 2` は書かない（P1 / P4）。

## 制約

Global: Kernel ネイティブ、Graphify/Serena 必須、VM コピー禁止、先取り禁止。P0 では Serena を「次セッションで activate できる状態」までにする。

## 受け入れ

- [ ] `PHASE` が `P0`
- [ ] `./scripts/test.sh` 緑
- [ ] `./build/ao --version` が空でない
- [ ] `.serena/project.yml` がある
- [ ] `graphify-out/GRAPH_REPORT.md` がある
- [ ] `docs/phases/P9.md` と `docs/prs/P9-04.md` がある

## PR 一覧

[P0-01](../prs/P0-01.md) … [P0-07](../prs/P0-07.md)

## Graphify / Serena

P0-06 で Serena、P0-07 で `/graphify .`。以降のセッションは GRAPH_REPORT を読んでから改修する。
